git lfs install && git lfs track "*.png"&& git lfs track "*.jpg"&& git lfs track "*.mp3"&& git lfs track "*.mp4"&& git lfs track "*.ttf"&& git lfs track "*.wav" && git add . && git commit -m "git lfs add" && git push origin master && git checkout -b develop  && git push origin develop && git checkout master